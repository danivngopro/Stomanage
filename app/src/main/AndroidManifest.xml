<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stomanage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Stomanage">
        <activity android:name=".WarehouseManagementScreen"></activity>
        <activity android:name=".NewFactory" />
        <activity android:name=".FactoryManagementScreen" />
        <activity android:name=".TroopManagerScreen" />
        <activity android:name=".register" />
        <activity android:name=".ManagerMainScreen" />
        <activity android:name=".Login" />
        <activity android:name=".itemList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>